{% extends "base.njk" %}

{% block content %}
<article class="post">
{% if hide_page_title != "true" %}
  <header class="post-header">
    <h1 class="mt0">{{ title }}</h1>
  </header>
  {% endif %}
  <section class="post-body stack">
    {{ content | safe }}
  </section>
</article>
{% endblock %}